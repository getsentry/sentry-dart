name: e2e_test
description: Simple program to test if an error is transferred to Sentry.io
version: 1.0.0

publish_to: none

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  sentry:
    path:
      ./../dart
  http: ^0.13.3

dev_dependencies:
  lints: ^1.0.1
