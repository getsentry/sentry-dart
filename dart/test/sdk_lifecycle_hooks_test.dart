// This tests that the base functionality of the hooks function correctly.
// They are implemented in `SentryClient`

void main() {
  late Fixture fixture;

  setUp(() {
    fixture = Fixture();
  });
}
