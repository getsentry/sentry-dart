name: sentry_dart_web_example
description: An absolute bare-bones web app.

publish_to: 'none'

environment:
  sdk: '>=2.17.0 <4.0.0'

dependencies:
  sentry:
    path: ../../dart

dev_dependencies:
  build_runner: ^2.1.8
  build_web_compilers: ^3.2.2
  lints: ^2.0.0
  webdev: ^2.7.6
