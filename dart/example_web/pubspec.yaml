name: sentry_dart_web_example
description: An absolute bare-bones web app.

environment:
  sdk: ^2.0.0

dependencies:
  sentry:
    path: ../

dev_dependencies:
  build_runner: ^1.10.0
  build_web_compilers: ^2.13.0
  pedantic: ^1.9.0
