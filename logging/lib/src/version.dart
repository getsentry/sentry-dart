/// The SDK version reported to Sentry.io in the submitted events.
const String sdkVersion = '6.18.0';

/// The package name reported to Sentry.io in the submitted events.
const String packageName = 'pub:sentry_logging';
